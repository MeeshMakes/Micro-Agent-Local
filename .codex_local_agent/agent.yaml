# Local autonomous agent config.
# High contrast rule: any UI that renders this text must use light-on-dark.
llm:
  provider: offline-ollama
  model: gpt-oss:20b
  endpoint: http://127.0.0.1:11434/v1/chat
policies:
  safe_write_outside_repo: false
  require_command_allow: true
authority:
  mode: Ask First